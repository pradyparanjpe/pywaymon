tooltip:
  sep: "│"
  max:
    row: 12
    col: 10
  clip:
    cell: 15
    text: 78

IO:
  tip_type: disks + pids

processor:
  # lowest: $ echo "100/$(nproc)" | bc
  tip_type: processors + pids

memory:
  tip_type: device + pids

temperature:
  ambient: 27  # Temperature in degrees Celsius

load:
  ignore_below:
    1: 12  # nprocs
    5: 9   # nprocs * 0.75
    15: 6  # nprocs * 0.5

netcheck:
  internet: 8.8.8.8  # Google's DNS as an indicator of connection to internet
  home_ap: [192.168.1.1]

netio:
  ignore_below: 1024  # [= 1kB/s] Ignore below this speed (bytes/s)
